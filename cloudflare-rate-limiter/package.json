{
  "name": "chat-gpt-tree-rate-limiter",
  "version": "1.0.0",
  "description": "Cloudflare Worker rate limiter for Gemini API",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}

